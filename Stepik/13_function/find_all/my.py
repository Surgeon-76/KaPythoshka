# объявление функции
def find_all(target, symbol):
    m = list(target)
    
    print(m)

    #return [i for i in range...]

# считываем данные
s = input()
char = input()

# вызываем функцию
find_all(s, char)
#print(find_all(s, char))